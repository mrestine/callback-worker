node_modules/
dist/

# env
.env
.env.local

# Gmail OAuth client secrets + cached token (bind-mounted into the container)
secrets/

# real emails and anything derived from them (keep the dir so the bind mount works)
fixtures/private/*
!fixtures/private/.gitkeep
**/*.out.json

# per-message model I/O debug logs (real email content) — keep the dir so the bind mount works
debug/*
!debug/.gitkeep

# os / logs
.DS_Store
*.log
